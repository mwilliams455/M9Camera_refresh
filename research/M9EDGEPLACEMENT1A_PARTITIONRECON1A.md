# M9EDGEPLACEMENT1A — PARTITIONRECON1A

Research-only ordinal reconstruction probe. This file does **not** authorize a live selector, does not modify the frozen renderer, and does not promote any identity except the already visually recognized `164048` DARK example.

## Confirmed / strongest anchor

The original reviewer recognized:

`IMG_20260904_164048_1788536448208_00`

as the retained **#73** scene: sculpture/statue against bright field/sky.

The rendered content independently matches that retained visual description. This exact frame remains a confirmed `DARK_FAIL`; the ordinal `#73` is treated as the strongest current visual-order anchor.

Within the recovered Part-3 chronological stem list, `164048` is local position **12**.

## Contiguous-partition hypothesis

If the original visual corpus preserved chronological JPEG-instance order through Part 3 with no extra JPEG instance inserted before local position 12, then:

`global ordinal = Part-3 local position + 61`

Under that hypothesis Part 3 spans approximately **#62–#91**.

This is a falsification hypothesis, not a promoted constant offset.

## Immediate predicted identities

The hypothesis predicts:

- `#73` -> local 12 -> `IMG_20260904_164048_1788536448208_00` — already visually recognized; anchor
- `#80` -> local 19 -> `IMG_20260904_164458_1788536698183_00` — must visually match retained `dark lane / bright background`
- `#86` -> local 25 -> `IMG_20260904_164647_1788536807335_00` — expected dense shaded-park/backlight boundary region
- `#87` -> local 26 -> `IMG_20260904_164707_1788536827532_00` — expected neighboring shaded-park/backlight boundary region
- `#98` -> outside Part 3; if Part 4 follows contiguously from #92, then Part-4 local position 7
- `#40` -> necessarily in Part 2, not Part 3
- `#4` -> necessarily in Part 1

## Why this is materially stronger than the rejected +30 mapping

The rejected mapping was inferred from non-unique q95/MFM telemetry similarity. PARTITIONRECON1A instead starts from a direct visual scene recognition (#73) and asks whether **other retained visual descriptions land on the predicted photographs**.

It is therefore explicitly falsifiable:

1. if predicted #80 does not look like the retained dark-lane / bright-background scene, reject the +61 hypothesis;
2. if predicted #86/#87 do not resemble the retained adjacent shaded-park/backlight boundary pair, reject or revise the hypothesis;
3. if actual ZIP JPEG-instance counts show an inserted/missing/duplicate instance before the anchor, recompute using JPEG instances rather than capture stems;
4. never use capture-stem count as a substitute for JPEG-instance order.

## 121 JPEG / 120 capture invariant

The full corpus is known to contain **121 JPEG instances but 120 complete capture/RAW sets**. Therefore final ordinal reconstruction must be done from actual JPEG instances across the re-uploaded archives. PARTITIONRECON1A is only a temporary structural probe while archive-byte enumeration is unavailable.

The single extra JPEG instance may shift ordinals after its position. It must be located before any global mapping is considered final.

## Current photographic labels remain independent

Directly reviewed Part-3 GOOD and BOUNDARY labels remain valid by exact `IMG_...` identity. Their previously inferred global ordinal numbers remain withdrawn.

`IMG_20260904_164019_1788536419887_00` remains an independent `REVIEW_DARK_CANDIDATE`; it is not #40 under this reconstruction and must not be promoted from telemetry alone.

## Required next step

Enumerate all JPEG members from:

- Part 1
- Part 2 + Part 2b
- Part 3
- Part 4

Sort by the original filename/instance order, explicitly preserve duplicate JPEG instances, locate the 121st JPEG discrepancy, and then test the predicted #80/#86/#87/#98 identities against the original visual labels.

No live APK EDGEPLACEMENT correction is authorized from this note.
