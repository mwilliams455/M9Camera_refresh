name: Build M9Cam LIVEWYSIWYG1H Post-Demosaic Surface

on:
  workflow_dispatch:
  pull_request:
    branches: [research/m9livewysiwyg1g-normalpriority]
    paths:
      - 'patches/apply-m9cam-m9livewysiwyg1h-postdemosaic-surface.py'
      - 'patches/verify-m9cam-m9livewysiwyg1h-postdemosaic-surface.py'
      - '.github/workflows/build-m9cam-m9livewysiwyg1h-postdemosaic-surface.yml'
  push:
    branches: [research/m9livewysiwyg1h-postdemosaic-surface]
    paths:
      - 'patches/apply-m9cam-m9livewysiwyg1h-postdemosaic-surface.py'
      - 'patches/verify-m9cam-m9livewysiwyg1h-postdemosaic-surface.py'
      - '.github/workflows/build-m9cam-m9livewysiwyg1h-postdemosaic-surface.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Verify LIVEWYSIWYG1H patch syntax
        run: |
          set -euo pipefail
          python3 -m py_compile patches/apply-m9cam-m9livewysiwyg1h-postdemosaic-surface.py
          python3 -m py_compile patches/verify-m9cam-m9livewysiwyg1h-postdemosaic-surface.py

      - name: Replay exact LIVEWYSIWYG1G assembled baseline
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          wf=Path('.github/workflows/build-m9cam-m9livewysiwyg1g-normalpriority.yml')
          lines=wf.read_text().splitlines()
          names=[
              'Verify LIVEWYSIWYG1G patch syntax',
              'Replay exact LIVEWYSIWYG1F assembled baseline',
              'Freeze photographic path before LIVEWYSIWYG1G',
              'Apply normal-priority scheduler',
              'Prove photographic path unchanged',
          ]
          for idx,name in enumerate(names):
              target='      - name: '+name
              if target not in lines:
                  raise SystemExit('LIVEWYSIWYG1G replay block missing: '+name)
              i=lines.index(target)
              while i<len(lines) and lines[i].strip()!='run: |':
                  i+=1
              if i>=len(lines):
                  raise SystemExit('LIVEWYSIWYG1G run block missing: '+name)
              i+=1
              out=[]
              while i<len(lines):
                  line=lines[i]
                  if line.startswith('      - name:') or line.startswith('      - uses:'):
                      break
                  if line.startswith('          '):
                      out.append(line[10:])
                  elif not line.strip():
                      out.append('')
                  else:
                      raise SystemExit('unexpected LIVEWYSIWYG1G indentation: '+line)
                  i+=1
              Path(f'/tmp/m9livewysiwyg1h_parent_{idx:02d}.sh').write_text('\n'.join(out)+'\n')
          PY
          for s in /tmp/m9livewysiwyg1h_parent_*.sh; do
            echo "=== replay $s ==="
            bash "$s"
          done

      - name: Freeze still assets and capture contract before LIVEWYSIWYG1H
        run: |
          set -euo pipefail
          ROOT="$GITHUB_WORKSPACE/PhotonCamera"
          sha256sum \
            "$ROOT/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java" \
            "$ROOT/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IsoExpoSelector.java" \
            "$ROOT/app/src/main/java/com/particlesdevs/photoncamera/m9/render/M9EdgePlacementBestFit2AController.java" \
            "$ROOT/app/src/main/cpp/m9color_jni.cpp" \
            "$ROOT/app/src/main/java/com/particlesdevs/photoncamera/m9/render/M9TargetFirmwareCalibration.java" \
            "$ROOT/app/src/main/assets/m9/m9_curve02_firmware.bin" \
            > LIVEWYSIWYG1H_FROZEN_BEFORE.sha256

      - name: Apply post-demosaic live surface
        run: |
          set -euo pipefail
          ROOT="$GITHUB_WORKSPACE/PhotonCamera"
          python3 patches/apply-m9cam-m9livewysiwyg1h-postdemosaic-surface.py "$ROOT"
          python3 patches/verify-m9cam-m9livewysiwyg1h-postdemosaic-surface.py "$ROOT" \
            | tee LIVEWYSIWYG1H_VERIFY.txt

      - name: Prove still assets and capture contract unchanged
        run: |
          set -euo pipefail
          ROOT="$GITHUB_WORKSPACE/PhotonCamera"
          sha256sum \
            "$ROOT/app/src/main/java/com/particlesdevs/photoncamera/capture/CaptureController.java" \
            "$ROOT/app/src/main/java/com/particlesdevs/photoncamera/processing/parameters/IsoExpoSelector.java" \
            "$ROOT/app/src/main/java/com/particlesdevs/photoncamera/m9/render/M9EdgePlacementBestFit2AController.java" \
            "$ROOT/app/src/main/cpp/m9color_jni.cpp" \
            "$ROOT/app/src/main/java/com/particlesdevs/photoncamera/m9/render/M9TargetFirmwareCalibration.java" \
            "$ROOT/app/src/main/assets/m9/m9_curve02_firmware.bin" \
            > LIVEWYSIWYG1H_FROZEN_AFTER.sha256
          diff -u LIVEWYSIWYG1H_FROZEN_BEFORE.sha256 LIVEWYSIWYG1H_FROZEN_AFTER.sha256

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Locate runner Android SDK
        run: |
          set -euo pipefail
          SDKMANAGER="$(command -v sdkmanager || true)"
          if [ -z "$SDKMANAGER" ]; then
            for candidate in "${ANDROID_HOME:-}/cmdline-tools/latest/bin/sdkmanager" "${ANDROID_SDK_ROOT:-}/cmdline-tools/latest/bin/sdkmanager" "/usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager"; do
              if [ -x "$candidate" ]; then SDKMANAGER="$candidate"; break; fi
            done
          fi
          if [ -z "$SDKMANAGER" ]; then
            SDKMANAGER="$(find /usr/local/lib/android/sdk -type f -name sdkmanager -perm -u+x 2>/dev/null | head -n 1 || true)"
          fi
          if [ -z "$SDKMANAGER" ] || [ ! -x "$SDKMANAGER" ]; then exit 1; fi
          SDKROOT="$(cd "$(dirname "$SDKMANAGER")/../../.." && pwd)"
          echo "ANDROID_HOME=$SDKROOT" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$SDKROOT" >> "$GITHUB_ENV"
          echo "$(dirname "$SDKMANAGER")" >> "$GITHUB_PATH"

      - name: Install Android build components
        run: |
          set -euo pipefail
          SDKMANAGER="$(command -v sdkmanager)"
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" "platforms;android-36" "build-tools;35.0.0" "cmake;3.22.1" "ndk;27.0.12077973"

      - name: Build LIVEWYSIWYG1H APK
        run: |
          set -o pipefail
          cd PhotonCamera
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace 2>&1 \
            | tee "$GITHUB_WORKSPACE/LIVEWYSIWYG1H_GRADLE.log"

      - name: Verify packaged LIVEWYSIWYG1H contract
        if: success()
        run: |
          set -euo pipefail
          APK="$(find PhotonCamera/app/build/outputs/apk/debug -name '*.apk' | head -n 1)"
          rm -rf /tmp/m9livewysiwyg1h-apk
          mkdir -p /tmp/m9livewysiwyg1h-apk
          unzip -q "$APK" -d /tmp/m9livewysiwyg1h-apk
          python3 - <<'PY' | tee LIVEWYSIWYG1H_APK_PROOF.txt
          from pathlib import Path
          root=Path('/tmp/m9livewysiwyg1h-apk')
          dex=b''.join(p.read_bytes() for p in sorted(root.glob('classes*.dex')))
          required=[
              b'FULL_SOURCE_PREPROCESS_POSTDEMOSAIC_TC20_SURFACE_FINAL_M9_BITMAP',
              b'after_full_RAW_normalization_shading_and_MHC_demosaic_before_final_M9_color_pass',
              b'm9LivePreview1HPostDemosaicSurface',
              b'M9LIVEWYSIWYG1F liveRotation=',
              b'M9LIVEWYSIWYG1G renderWallMs=',
              b'M9LIVEWYSIWYG1B_DISPLAYEDCAPTURELOCK',
              b'setExactExposureM9Wysiwyg1B',
          ]
          for marker in required:
              ok=marker in dex
              print(marker.decode('ascii'),ok)
              if not ok:
                  raise SystemExit('missing APK marker '+marker.decode('ascii'))
          for marker in (
              b'M9LIVEWYSIWYG1D_METER1600_REFERENCE_RENDER',
              b'FULL_PRODUCTION_RENDER_REFERENCE1600_FINAL_BITMAP',
              b'reduceBayerMeterReference1600PreservingParity1D'):
              if marker in dex:
                  raise SystemExit('invalid 1D marker packaged '+marker.decode('ascii'))
          print('LIVEWYSIWYG1H APK PROOF PASS')
          PY

      - name: Record APK SHA256
        if: success()
        run: |
          sha256sum PhotonCamera/app/build/outputs/apk/debug/*.apk \
            | tee LIVEWYSIWYG1H_APK_SHA256.txt

      - name: Upload validation APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: M9Cam-LIVEWYSIWYG1H-POSTDEMOSAIC
          path: |
            PhotonCamera/app/build/outputs/apk/debug/*.apk
            LIVEWYSIWYG1H_APK_SHA256.txt
            LIVEWYSIWYG1H_VERIFY.txt
            LIVEWYSIWYG1H_APK_PROOF.txt
            LIVEWYSIWYG1H_FROZEN_BEFORE.sha256
            LIVEWYSIWYG1H_FROZEN_AFTER.sha256

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: M9-LIVEWYSIWYG1H-POSTDEMOSAIC-BUILDLOG
          path: LIVEWYSIWYG1H_GRADLE.log
          if-no-files-found: ignore
